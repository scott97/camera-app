package main

func main() {
	startWebServer()

	// if err := rpio.Open(); err != nil {
	// 	fmt.Println(err)
	// 	os.Exit(1)
	// }
	// defer rpio.Close()

	// var pin = rpio.Pin(17)
	// pin.Output()

	// for x := 0; x < 20; x++ {
	// 	pin.Toggle()
	// 	time.Sleep(time.Second)
	// }
}
